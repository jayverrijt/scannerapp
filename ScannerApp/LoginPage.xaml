<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ScannerApp.LoginPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Entry Grid.Row="0" Grid.Column="0" x:Name="userBox" IsPassword="False" Placeholder="Username"></Entry>
            <Entry Grid.Row="1" Grid.Column="0" x:Name="passBox" IsPassword="True" Placeholder="Password"></Entry>
            <Button Grid.Row="2" Grid.Column="0" x:Name="loginBtn" Clicked="LoginBtn_OnClicked" Text="Login"></Button>
        </Grid>
        
    </ContentPage.Content>
</ContentPage>